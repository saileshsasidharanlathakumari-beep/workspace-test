# Copy to .env (Vite reads VITE_* at start; restart `npm run dev` after edits).

# true = local-nodegraph (no JWT). false = cloud proxy (needs Auth0).
VITE_LOCAL_ENGINE=true

VITE_PROGRAM_ID=nodegraph:1788533000471
VITE_PROJECT_ID=c8085f9b-348b-41e5-b8c4-9872e34be9ba
